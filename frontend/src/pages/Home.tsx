import React from "react";
import { useMeetingStore } from "../stores/MeetingStore";

const Home = () => {
  // Meeting list will just be queried for at some endpoint
  //const { meetingList };
  // use effect, whenever meeting list changes
  return <></>;
};

export default Home;
